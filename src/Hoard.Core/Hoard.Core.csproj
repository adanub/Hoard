<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.9" />
    <!-- Direct override: the 2.1.11 native SQLite that EF pulls in has a known vulnerability
         (GHSA-2m69-gcr7-jv3q) with no patched 2.x — the 3.x line carries the fixed SQLite. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.3" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="10.0.9" />
    <!-- No SkiaSharp here on purpose. Core is the assembly a future mobile head and sync server
         reference, so it stays free of native graphics binaries; the only Skia code in the repo is the
         desktop GIF decoder, and the package is declared there. -->
  </ItemGroup>

</Project>
