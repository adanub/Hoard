# Keeps the SHA-pinned actions in .github/workflows fresh — with actions
# pinned to full commits, Dependabot PRs are the intended update path.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
